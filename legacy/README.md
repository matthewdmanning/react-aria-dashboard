# Legacy quarantine

The rejected component registry, universal dashboard configuration, proposal/preview MCP flow, formatter-output validation, and panel-owned persistence callback remain recoverable from commit `5ac30e8`.

Nothing in this directory is an active requirement, dependency, example, build input, test input, or agent route.
